// Shared layout constants for CLI rendering.

/** Width (in cells) of the per-row magnitude bars in "by area" / "by package". */
export const BAR_WIDTH = 24;

/** Terminal width is clamped to this range for full-width bars/headers. */
export const MIN_TERM_WIDTH = 40;
export const MAX_TERM_WIDTH = 80;

/** Max width of the framed diagnosis card. */
export const MAX_CARD_WIDTH = 74;

/** Canonical homepage, used for the wordmark hyperlink. */
export const HOMEPAGE = "https://move.doctor/";

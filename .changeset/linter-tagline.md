---
"move-doctor": patch
---

Refresh package branding: new tagline "A deterministic linter for Sui Move", a clearer npm `description`, tidied `keywords` (dropped the inaccurate `audit` tag), and a README with up-to-date sample output and source-linked rule buckets.

[package]
name = "conventions_bad"
# edition deliberately missing — should trigger missing-edition-2024

[dependencies]
Sui = { local = "../sui" }
MoveStdlib = { local = "../move-stdlib" }
SomeOtherDep = { local = "../other" }

[addresses]
math = "0x0"
conventions_bad_main = "0x0"
